# Copy to .env.local (gitignored) and fill in. Never shipped in the app bundle.
# Used by `npm run fetch:samples`, and by the dev / preview servers to answer the in-app Pexels photo search
# (they proxy ./api/pexels/* and add the key server-side). Other builds ask each user for their own free key.
PEXELS_API_KEY=
